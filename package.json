{
  "name": "portfolio",
  "version": "0.0.0",
  "description": "Vite-powered portfolio site",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "sass-embedded": "^1.79.2",
    "vite": "^5.4.6"
  }
}
